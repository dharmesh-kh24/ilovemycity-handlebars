<!DOCTYPE html>
<html>

<head>
    {{>head}}
</head>

<body>
    {{>navbar}}
    <div class="container">
        {{>heading}}
        <div class="row">
            {{{body}}}
        </div>
    </div>
    {{>scripts}}
</body>

</html>
